"""Validation harnesses (project_plan.md §7).

- :mod:`.rover_rediscovery`     — the headline validation (Layer 5).
- :mod:`.experimental_comparison` — sub-model vs published experimental data
  (Layer 3) and mission evaluator vs real-rover traverse logs (Layer 4).
- :mod:`.error_budget`          — layered error-budget compilation.
"""
