"""Tradespace exploration layer.

- :mod:`.sweeps`        — parametric sweeps + interactive Jupyter widgets.
- :mod:`.optimizer`     — NSGA-II via pymoo, wrapping the surrogate.
- :mod:`.design_rules`  — SHAP global importance + partial dependence.
- :mod:`.visualize`     — shared plotting utilities (Pareto fronts, PDPs).
"""
